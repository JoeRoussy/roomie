const config = {
    // user: {
    //     name: 'Joe',
    //     email: 'joeroussy@gmail.com'
    // }
};

const userReducer = (state = config, actions) => {


    return state;
}

export default userReducer;
