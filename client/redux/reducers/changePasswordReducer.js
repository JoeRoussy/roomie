const config = {};

const changePasswordReducer = (state = config, actions) => {
    const {
        type,
        payload
    } = actions;

    // switch(type) {
    //
    // }

    return state;
}

export default changePasswordReducer;
